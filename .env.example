# ============================================================
# PH4 Backend - Environment Configuration
# ============================================================

# -------------------- Database --------------------
# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/ph4

# -------------------- JWT Authentication --------------------
# JWT secret key (REQUIRED - must be 32+ characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-production-secret-key-minimum-32-characters-long

# JWT token expiry (default: 7d)
JWT_EXPIRE=7d

# -------------------- Server --------------------
# Server port
PORT=5055

# Node environment
NODE_ENV=development

# -------------------- Security --------------------
# CORS allowed origins (comma-separated, no spaces)
CORS_ORIGINS=http://localhost:19000,http://localhost:8081,http://localhost:3000

# CORS credentials (set to true if cookies/auth needed)
CORS_CREDENTIALS=false

# Trust proxy headers (set to true if behind nginx/CloudFlare/AWS ALB)
TRUST_PROXY=false

# -------------------- Sentry (Production Error Tracking) --------------------
# Enable/disable Sentry (default: true in production, false in development)
SENTRY_ENABLED=false

# Sentry DSN from https://sentry.io
# Example: https://abc123@o123456.ingest.sentry.io/7890123
SENTRY_DSN=

# Sentry environment name (default: from NODE_ENV)
SENTRY_ENVIRONMENT=development

# Sentry performance monitoring sample rate (0.0 to 1.0)
# 0.0 = disabled, 0.1 = 10% of transactions, 1.0 = 100%
# Recommended: 0.0 in dev, 0.1-0.2 in production to save quota
SENTRY_TRACES_SAMPLE_RATE=0.0

# -------------------- Logging --------------------
# Winston log level (debug, info, warn, error)
LOG_LEVEL=info

# Log to console (set to false in production)
LOG_CONSOLE=true

# ============================================================

# -------------------- Public Bill Share Links --------------------
# Public base URL for shareable bill links (REQUIRED in production)
# Example: https://www.profithooks.com
# In dev, this can be omitted (will fallback to request host)

# -------------------- Public App Base URL (Web Frontend) --------------------
# Web frontend base URL for shareable bill links (REQUIRED in production)
# Share links point to: {PUBLIC_APP_BASE_URL}/b/:token
# Dev default: http://localhost:5173 (Vite dev server)
# Production: https://www.profithooks.com
PUBLIC_APP_BASE_URL=http://localhost:5173
